# Core (required) — the default offline pipeline runs on these alone plus
# the Python standard library and numpy.
numpy>=1.24
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0

# Testing
pytest>=8.0
httpx>=0.27          # used by FastAPI's TestClient

# --- Optional / pluggable upgrades -----------------------------------------
# Uncomment any of these to swap in the heavier, higher-fidelity backend.
# The code auto-detects and uses them when installed + selected via env vars
# (see src/config.py and README.md "Pluggable backends").

# scikit-learn>=1.4          # faster/more-complete TF-IDF (EMBEDDING_BACKEND=tfidf uses this if present)
# sentence-transformers>=2.6 # dense embeddings (EMBEDDING_BACKEND=sentence-transformers)
# faiss-cpu>=1.8             # vector index (VECTOR_INDEX_BACKEND=faiss)
# langchain>=0.2             # document loading / chunking / prompt templates (see src/langchain_variant.py)
# langchain-community>=0.2
# langchain-openai>=0.1
# openai>=1.30                # LLM_BACKEND=openai (requires OPENAI_API_KEY)
